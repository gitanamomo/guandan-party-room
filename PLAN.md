# 掼蛋实施方案

## 产品边界

HTML 搭载、本地运行、不部署公网。每位知道口令 gina 的参与人均可创建房间或凭房间编号加入；创建者是该房房主。共享口令是入场凭据，不是假设一个只能一人登录的主账号。四座满员且全员准备后发牌，对座为队友；禁止客户端获取别人手牌。

同一 Wi-Fi 为默认提案，待用户确认。多人状态必须由主机服务同步，不能只靠单文件或 localStorage。建议 Python 标准库本地 HTTP 服务加短轮询，前端纯 HTML/CSS/JS，3D 库与资源本地保存。服务仅暴露专用静态文件及明确 API；牌局内存保存，重启即结束，正式说明中需标明。主机保持运行且 Wi-Fi 禁用客户端隔离才能加入。异地采用私有网络方案，需单独确认。

## 第一批：Function 设计确认（历史）

2026-09-07 用户反馈：“构图可以，人物和场景需要再精致些”。构图认可已记录。第二版精修图已生成并嵌入 HTML，等待美术审阅；不将概念图冒充可导出三维资产。前景人物在概念图中遮挡部分桌面，正式运行镜头需降低或移走前景角色，并重新校验完整出牌区可视性。

8 × 8 × 3.6 米暖色会所；中心 3.2 × 2.4 米圆角牌桌；四座朝中心。主镜头固定斜俯视，手牌由 HTML 覆盖，手持设备优先横屏。场景角色为原创大头小身比例，面部避免复制既有人物。双边对称；主镜头隐藏近侧遮挡结构为已声明例外。无自由行走，人物直接入座；不需要玩家可通行主入口，明确作为功能例外。窗户后续需要真实开孔与窗台。

预算目标：场景 ≤ 80,000 三角形，低档资源 ≤ 5 MB，高档纹理 ≤ 16 MB。Mac 目标 60 fps，手机目标 30 fps，均待实测。付费服务预算为 0，暂不提交 Meshy。当前 CSS 立体示意仅用于构图讨论，不充当正式三维模型证据。尚缺三维几何正投影、参考资产接触表、正式开孔证据；Function 不得标为通过。

## 第二批：规则与联机核心

完整 108 张牌、每人 27 张、四人两队；按圈轮流，服务端验证持牌、轮次与合法牌型。实现单张、对子、三张、三带二、五张顺子、三连对、两连三、同点炸弹、同花顺及四王炸；红桃级牌作为百搭。顺子边界、百搭能否替王等细则在 RULES.md 固定后测试；不使用随意猜测的牌型实现。

开房、加入、准备、离开、倒计时提示、房号复制、音效开关。加入失败给出原因；可重连时恢复原座而非重复占座。会话令牌随机生成，加入和出牌接口都鉴权，口令尝试限速；房间状态仅返回各玩家有权看到的信息。

## 第三批：完整赛制及验收

从打 2 开始，记录队伍级数、头二三末游，处理升级、接风、贡牌、还贡、抗贡和打 A 结束规则。地方规则差异先明确为固定版本，不宣称通用所有玩法。出牌动作、纸牌声和可选语音；系统无中文语音时保留基础音效。

用规则测试覆盖百搭、炸弹优先级、顺子边界、非法出牌、队伍结算、贡还牌；用四个独立会话测试整局、错误口令、座位争抢、重连及隐私。检查 Safari / iOS 布局、触摸选牌及音频解锁；由真实第二台设备验收分享链接。

## 后续

掼蛋验收后复用房间、人物和音效制作斗地主，再制作麻将。每个阶段更新 README 和改动记录，提交前确认。暂无远程仓库，不推送或发布。退出仅清理本任务自己启动的进程和临时文件。

## 2026-09-08 本机规则阶段

v2已落实到可玩本机掼蛋：1名玩家与3名电脑、完整出牌及升级/贡还/冲A流程。规则版本见RULES.md。23组规则测试与40副自动对局通过，浏览器已验证选牌、出牌扣牌、电脑响应、弹窗和390px布局。

已实现同 Wi-Fi 最小权威房间，尚待四个独立浏览器/真机完整对局验收；不含重连和 AI 补位，未变更3D审批状态。Safari/iOS真机与音效实听待验收。Git仓库及远程尚未建立，提交仍需先确认。
