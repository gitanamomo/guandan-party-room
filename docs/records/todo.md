# 掼蛋小院项目验收清单

## 本轮已完成

| 项目 | 状态 | 验收结果 |
|---|---|---|
| 房主席位身份 | 已完成 | 创建房间后浏览器保存服务端返回的 `hostGuestId`，房主稳定匹配0号席位。 |
| 房主重复入座保护 | 已完成 | 即使旧版浏览器带随机访客ID再次进入，服务端也会按房主账号或有效房主令牌定位已有房主席位，不会占用第二席位。 |
| 四人在线开局 | 已完成 | 房主加三位牌友后席位为0、1、2、3，房主可以正常开始对局，四家均发27张牌。 |
| 房间密码 | 已完成 | 开房可设置密码，入座和房间详情均强制校验；房主可在管理面板修改或取消密码。 |
| 观战权限 | 已完成 | 开房可设置是否允许观战，房主可在管理面板切换；禁止观战时未入座访客被拦截。 |
| 踢人 | 已完成 | 只有房主可请离非房主席位，被请离席位清空后可重新入座。 |
| 房主转让 | 已完成 | 只有房主可转让给已入座真人玩家，生成新的控制凭证并复制交接链接，原房主失去管理权限。 |
| 战绩、进贡与战报 | 已完成 | 原有多局记分、进贡还牌、国潮战报、方言报牌和古筝音乐链路保持通过。 |
| 互动动画与短音效 | 已完成 | 中央牌桌脉冲、出牌弹入、激活席位浮动、贴纸漂浮淡出；选牌、出牌、过牌、聊天、贴纸、入座和开局均有 Web Audio 反馈，并支持减少动态效果。 |

## 自动化验收

- [x] 非房主账号开局请求被拒绝。
- [x] 错误房主控制令牌开局请求被拒绝。
- [x] 旧版随机房主访客ID不会造成重复占位。
- [x] 匿名房主有效控制令牌可恢复0号席位。
- [x] 三位牌友正常入座，四家席位完整。
- [x] 合法房主成功开局，四家均为27张牌。
- [x] `pnpm check`、`pnpm test`、`pnpm build` 全部通过。
- [x] 浏览器确认大厅和四人牌桌页面正常加载。
- [x] 互动增强后的 `pnpm check`、`pnpm test`、`pnpm build` 全部通过。

## 最近测试数据

最近一次四人在线回归房间号：`865424`。该房间验证了房主席位恢复、三位牌友入座和房主开局。Vercel 生产 API 已验证主页与 tRPC 健康响应；房间创建是否可用取决于用户在 Vercel Production 环境配置 `DATABASE_URL`。

## 后续建议

建议后续使用正式长连接同步替代当前轮询，增加断线重连、房主转让后的旧链接失效提示，以及更完整的报单、报双、报三和最后一手方言语音。Vercel 部署时必须优先配置 `DATABASE_URL`，否则只能加载页面和无状态 tRPC 接口，无法创建在线房间。
